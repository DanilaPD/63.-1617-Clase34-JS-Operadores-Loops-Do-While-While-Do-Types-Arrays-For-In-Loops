let a = 5;
let b = 8;

//* Asignación haciendo la operación de una:

console.log((a += b));

//* Resto:

let c = 15;

//* Divido "c" por "2", pero me quedo lo que sobra:
console.log(c % 2);

//* If/Else más operadores lógicos:
if (a >= b && a === b) {
  console.log("a es igual a b");
} else if (a >= b || a === b) {
  console.log("a es mayor o igual a b");
} else {
  console.log("a es menor que b");
}
